identity:
  name: ota_fail_diagnose
  author: Dify
  label:
    en_US: OTA failure/timeout diagnose
    zh_Hans: OTA失败/超时问题诊断
    pt_BR: OTA failure/timeout diagnose
  icon: icon.svg
description:
  human:
    en_US: A tool for diagnose firmware upgrade(OTA) problems such as upgrade failure, upgrade timeout.
    zh_Hans: 一个用于诊断设备升级(OTA)失败、升级(OTA)超时问题的工具。
    pt_BR: A tool for diagnose firmware upgrade(OTA) problems such as upgrade failure, upgrade timeout.
  llm: 一个用于诊断设备升级(OTA)失败、升级(OTA)超时问题的工具。
parameters:
  - name: dev
    type: string
    required: true
    label:
      en_US: Device
      zh_Hans: 设备
      pt_BR: Device
    human_description:
      en_US: Device for diagnose
      zh_Hans: 需要诊断的设备ID
      pt_BR: Device for diagnose
    llm_description: device for diagnose
    form: llm
